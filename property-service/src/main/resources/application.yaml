server:
  port: 8086

spring:
  application:
    name: PROPERTY-SERVICE

  datasource:
    url: jdbc:mysql://mysql:3306/property_db
    username: nagesh
    password: nagesh123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka